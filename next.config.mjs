/** @type {import('next').NextConfig} */
const nextConfig = {
    devIndicators: {
      buildActivity: false, // ✅ disable the overlay (Loading / Compiling etc.)
    },
  };
  
  export default nextConfig;
  